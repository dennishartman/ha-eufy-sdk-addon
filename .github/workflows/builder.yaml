name: Builder

env:
  MONITORED_FILES: "config.yaml Dockerfile build.yaml run.sh"

on:
  # Publish images only when a GitHub Release is published. Channel split by pre-release flag:
  #   full release  → stable apps  (eufy_sdk_bridge      → addon-eufy-sdk-bridge)
  #   pre-release   → dev apps      (eufy_sdk_bridge_dev  → addon-eufy-sdk-bridge-dev)
  release:
    types: [published]
  workflow_dispatch:
  # PRs build every changed app with --test (no push) as a pre-merge check.
  pull_request:
    branches:
      - main
      - dev

permissions:
  contents: read

jobs:
  init:
    name: Initialize builds
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.filter.outputs.changed }}
      changed_apps: ${{ steps.filter.outputs.changed_apps }}
    steps:
      - name: Check out the repository
        uses: actions/checkout@v7.0.1

      - name: Get changed files
        id: changed_files
        if: github.event_name == 'pull_request'
        uses: tj-actions/changed-files@v47

      - name: Find app directories
        id: apps
        uses: home-assistant/actions/helpers/find-addons@master

      - name: Filter changed apps
        id: filter
        env:
          EVENT_NAME: ${{ github.event_name }}
          PRERELEASE: ${{ github.event.release.prerelease }}
          APPS: ${{ steps.apps.outputs.addons }}
          CHANGED_FILES: ${{ steps.changed_files.outputs.all_changed_files }}
        run: |
          changed_apps=()

          if [[ "${EVENT_NAME}" == "release" ]]; then
            # Channel split: a FULL release publishes the stable apps; a PRE-release publishes the dev
            # channel (the `*_dev` apps). Keeping them apart stops a beta from moving the stable image
            # (and stops a stable release from re-publishing dev). GitHub fires `published` for both.
            for app in ${APPS}; do
              if [[ "${PRERELEASE}" == "true" && "${app}" == *_dev ]]; then
                changed_apps+=("${app}")
              elif [[ "${PRERELEASE}" != "true" && "${app}" != *_dev ]]; then
                changed_apps+=("${app}")
              fi
            done
          elif [[ "${EVENT_NAME}" == "workflow_dispatch" ]]; then
            # A manual run rebuilds every app (test-only — publish is gated to release events below).
            changed_apps=(${APPS})
          elif [[ "${CHANGED_FILES}" =~ \.github/workflows/(builder|build-app)\.yaml ]]; then
            # If the workflow file itself changed, rebuild all apps
            changed_apps=(${APPS})
          else
            for app in ${APPS}; do
              for file in ${MONITORED_FILES}; do
                if [[ "${CHANGED_FILES}" =~ ${app}/${file} ]]; then
                  changed_apps+=("${app}")
                  break
                fi
              done
            done
          fi

          if [[ ${#changed_apps[@]} -gt 0 ]]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "changed_apps=$(jq -nc '$ARGS.positional' --args "${changed_apps[@]}")" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

  build-app:
    name: Build ${{ matrix.app }}
    needs: init
    if: needs.init.outputs.changed == 'true'
    permissions:
      contents: read
      id-token: write
      packages: write
    strategy:
      fail-fast: false
      matrix:
        app: ${{ fromJSON(needs.init.outputs.changed_apps) }}
    uses: ./.github/workflows/build-app.yaml
    with:
      app: ${{ matrix.app }}
      publish: ${{ github.event_name == 'release' }}
    secrets: inherit
